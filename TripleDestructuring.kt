fun main() {
    fun main() {
        val firstUser : Triple<String, Byte, Int> = Triple("User 1", 20, 1)

        val (username, age, decision) = firstUser

        println(username)
        println(age)
        println(decision)

    }
}